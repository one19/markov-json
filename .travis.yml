language: node_js
sudo: false
node_js:
  - '10'
  - '9'
  - '8'
  - '7'
  - '6'
install:
  - yarn
  - npm i codecov -g
cache:
  yarn: true
  directories:
    - node_modules
script:
  - tsc && nyc ava dist/test
after_success:
  - nyc report --reporter=text-lcov > coverage.lcov && codecov